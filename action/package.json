{
  "name": "reproforge-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action wrapper for ReproForge CI",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/postbuild.cjs",
    "verify": "node dist/index.js --self-test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
